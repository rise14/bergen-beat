/**
 * Root default Open Graph image (homepage and any segment without its own).
 * Shared implementation lives in components/og/DefaultOGImage.tsx.
 */
export { default, size, contentType, alt } from "@/components/og/DefaultOGImage";
